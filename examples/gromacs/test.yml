steps:
- pdb:
    in:
      # output_pdb_path: "&protein_models.pdb"
      config: 
        pdb_code: '1auo' 
- extract_model:
    in:
      config:
        models: [1]
    #  input_structure_path: '*protein_models.pdb'
      output_structure_path: '&protein.pdb'